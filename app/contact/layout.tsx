// app/contact/layout.tsx
export const metadata = {
  title: "Contact | Gothic Walks Savannah",
  description: "Get in touch with Gothic Walks.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}